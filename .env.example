# Django Settings
DJANGO_SETTINGS_MODULE=config.settings.production

# Secret Key (generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())")
SECRET_KEY=63qtb!-)x=s%81&mz$$o1k4^85si0rp1z#9xov=dzg15ek$43q

# Debug Mode
DEBUG=False

# Allowed Hosts (for Railway deployment)
ALLOWED_HOSTS=gym-tracker-backend.up.railway.app

# Database URL (Railway provides this automatically, or use your PostgreSQL connection string)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/gym_tracker

# CORS Configuration
CORS_ALLOWED_ORIGINS=https://gym-tracker-frontend.vercel.app

# CSRF Configuration
CSRF_TRUSTED_ORIGINS=https://gym-tracker-backend.up.railway.app,https://gym-tracker-frontend.vercel.app

# AI Services
GROQ_API_KEY=get-from-groq-console